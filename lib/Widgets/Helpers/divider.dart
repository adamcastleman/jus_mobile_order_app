import 'package:flutter/material.dart';

class JusDivider {
  thick() {
    return const Divider(
      color: Colors.black,
      thickness: 0.5,
    );
  }

  thin() {
    return const Divider(
      thickness: 0.5,
    );
  }
}
